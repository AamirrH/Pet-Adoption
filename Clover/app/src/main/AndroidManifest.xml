<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clover">

        <!-- Splash — LAUNCHER (P1) -->
        <activity android:name=".SplashActivity" android:exported="true"
            android:theme="@style/Theme.Clover.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration (P7) -->
        <activity android:name=".RegistrationActivity" android:exported="false" />

        <!-- Home — Browse Pets (P2) -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- Pet Detail (P5) -->
        <activity android:name=".PetDetailActivity" android:exported="false" />

        <!-- Adoption Form (P3, P4, P6) -->
        <activity android:name=".AdoptionFormActivity" android:exported="false" />

        <!-- My Applications (P8) -->
        <activity android:name=".MyApplicationsActivity" android:exported="false" />

        <!-- Quiz (P6) -->
        <activity android:name=".QuizActivity" android:exported="false" />

        <!-- Settings (P7) -->
        <activity android:name=".SettingsActivity" android:exported="false" />

    </application>
</manifest>